sudo: required

language: node

services:
  - docker

install:
  - docker --version  # document the version travis is using
  - docker build -t docker-pimatic .

script:
  - docker run --rm docker-pimatic nodejs -v
#after_success:

