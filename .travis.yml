sudo: required

language: node

services:
  - docker

install:
  - docker --version  # document the version travis is using
  - docker build -t docker-pimatic .

script:
  - timeout 300s docker run --rm docker-pimatic
#after_success:

